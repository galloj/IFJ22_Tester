<?php
declare(strict_types=1);

$max = 0;
$in = readi();
while($in !== null) {
    $sum = 0;
    while($in !== null) {
        $sum = $sum + $in;
        $in = readi();
    }
    if($sum > $max) {
        $max = $sum;
    } else {}
    $in = readi();
}

write($max, "\n");